# Avaliacao de riscos — Story 3.1

## Resultado

Gate: **CONCERNS**.

## Evidencias

- 39 testes executados com sucesso, incluindo 10 testes especificos de `apps.service_orders`.
- `python manage.py check` passou.
- `python -m ruff check .` e `python -m ruff format --check .` passaram.
- `python manage.py spectacular --validate` passou.
- O endpoint exige token e valida que o equipamento pertence ao cliente informado.

## Riscos encontrados

1. **Contrato OpenAPI:** o componente gerado para os valores de status e `StatusEnum`, mas a arquitetura e o AC 11 especificam `ServiceOrderStatus`. Isso pode afetar consumidores que dependam do nome do componente no schema.
2. **Migrations:** `python manage.py makemigrations --check --dry-run` acusa `0002_alter_equipment_options.py` para o app `equipment`. A divergencia e anterior a Story 3.1, porem deixa o estado do projeto inconsistente para novas entregas.

Nenhum risco critico de seguranca ou persistencia foi identificado na implementacao da abertura de ordem.
